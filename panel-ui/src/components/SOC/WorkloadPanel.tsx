import React from 'react';
const WorkloadPanel:React.FC<{states:{label:string;value:number}[]}> = ({states}) => {const total=Math.max(1,states.reduce((a,b)=>a+b.value,0));return <section className="soc-panel analytics-panel"><div className="panel-heading"><div><span className="eyebrow">Operations</span><h2>Case Workload State</h2></div><span className="panel-total">{total} cases</span></div><div className="workload-bar">{states.map((s,i)=><i key={s.label} className={`workload-${i}`} style={{width:`${s.value/total*100}%`}} />)}</div><div className="workload-list">{states.map((s,i)=><div key={s.label}><span><i className={`workload-${i}`} />{s.label}</span><strong>{s.value}</strong><small>{Math.round(s.value/total*100)}%</small></div>)}</div></section>};
export default WorkloadPanel;
